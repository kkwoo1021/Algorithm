#18353.병사 배치하기(실2)
# 가장 긴 증가하는 부분수열
#가장 작은 것부터 뒤에 것 버블처럼 계산

n= int(input())
dp=[1]*n
soldier = list(map(int, input().split()))

soldier.reverse()

for i in range(1,n):
  for j in range(0,i):
    if soldier[j]<soldier[i]:
      dp[i]= max(dp[i],dp[j]+1)

print(n-max(dp))
